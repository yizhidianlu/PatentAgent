# ---------------------------------------------------------------------------
# 运行时数据 —— 切勿提交！
# data/app.db 内含**明文 API Key**，以及全部案件、上传原件与交付物。
# ---------------------------------------------------------------------------
data/
*.db
*.db-wal
*.db-shm

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# ---------------------------------------------------------------------------
# Node / 前端
# ---------------------------------------------------------------------------
node_modules/
frontend/dist/
*.tsbuildinfo
.vite/

# ---------------------------------------------------------------------------
# 环境与密钥
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
backend/.env

# ---------------------------------------------------------------------------
# 编辑器与操作系统
# ---------------------------------------------------------------------------
.idea/
.vscode/
*.swp
Thumbs.db
ehthumbs.db
desktop.ini
.DS_Store

# ---------------------------------------------------------------------------
# 日志与临时文件
# ---------------------------------------------------------------------------
*.log
tmp/

# ---------------------------------------------------------------------------
# Claude Code 的本机配置
#
# 源码机与部署机对它的需求不是一回事：部署机要给 update.ps1 / stop-serving.ps1
# 这类运维命令配权限白名单，源码机不跑那些。共享一份反而互相干扰。
#
# 不忽略的话，部署端一写配置，工作区就脏，update.ps1 判定「部署端不应有本地改动」
# 直接退出码 2 —— 于是 SYNC_PROTOCOL §1.1「可以改本机配置」和 §1.2「工作区必须
# 干净」这两条规则打起来了。忽略掉，两边各配各的。
# ---------------------------------------------------------------------------
.claude/
