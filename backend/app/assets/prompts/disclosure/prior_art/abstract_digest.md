---
source_repo: handsomestWei/patent-disclosure-skill
source_path: prompts/disclosure/prior_art_search.md（§A.6 abstract 必用、§A.7 链接与著录、「分析要求」「记录习惯」）
source_url: https://github.com/handsomestWei/patent-disclosure-skill
treatment: adapted
ported_version: 2026-08-25.1
notes: prior_art_search.md 拆分之二。「abstract 必用」三条与分析要求逐字保留；「缺失时」的补救途径由「改用详情页/Google Patents 补全」改为「保守概括并显式注明未核验」（平台无浏览能力，防编造）；浏览器验证 URL 改为「只准照抄 link 字段」；新增 JSON 输出契约（每批≤8条）。
---

# 查新 · 命中条目消化（Step 5 · STRUCT 调用）

## 任务

平台已完成国知局公布站检索（或收录了用户手工补录的在先文献），本批注入 ≤ 8 条命中条目（字段含 `pub_number` / `title` / `applicant` / `link` / `abstract` 等）。你的任务：逐条**消化改写**，产出查新笔记条目，供后续 1.1 撰写使用。仅输出一个 JSON 围栏块。

## `abstract` 字段（国知局条目，规定必用）

若某条含非空的 **`abstract`**（解析自公布站结果页摘要），对**该条专利**须同时遵守：

   - **必用**：查新笔记、交底书 **1.1** 中对该专利的**技术方案概括、应用场景与局限性分析**，**必须先基于对该 `abstract` 的完整阅读与理解**后再撰写；**禁止**仅凭标题、公开号或 URL **臆造**方案要点或与摘要矛盾的表述。
   - **充分理解**：在写入 1.1 或查新笔记前，须在**推理过程内**明确：摘要所涉**技术领域、解决什么问题、核心手段/模块、主要效果或流程**；若摘要与标题存在差异，**以摘要为准**概括该技术。
   - **正文呈现**：交底书 1.1 中**不得**大段逐字粘贴官方摘要（避免抄袭与超字数）；应**消化后**用**自己的话**压缩为「方案概括 + 应用 + 缺点/局限」；查新笔记可保留稍长的摘录供自用核对，但须标注来源于公布站摘要。
   - **缺失时**：若某条 JSON **无** `abstract` 或为空（旧版页面 / 表格布局未解析到等），须在查新笔记中注明「该条无摘要字段」，该条仅按标题与著录项**保守概括**并显式注明「无摘要，方案细节未核验」，**不得**杜撰技术细节或留空理由含糊带过。

## 链接与著录

**链接与著录**：命中项的「来源链接」**直接使用该条的 `link` 字段**（国知局公布站）；**禁止编造**。**不得**用 Google Patents URL **替换**已有 `link`。Google Patents 地址仅用于用户手工补录、本身无国知局 `link` 的条目。

## 分析要求

对检索到的、与方案**高度相关**的现有专利或公开文献逐项概括：

- 专利号 / 文献标识
- 技术方案要点（**若为国知局 JSON 且含 `abstract`，要点须与摘要理解一致**，见上文「`abstract` 必用」）
- 应用场景
- **局限性**
- **公开源 URL（必填）**：每一条必须附带**至少一个可公开访问、与著录项一致**的链接，写入查新笔记与交底书 1.1，便于代理人复核。**禁止编造或猜测 URL**；URL 只准**照抄**注入条目的 `link` 字段（服务端将校验产物 URL ∈ 命中链接集合）。

便于写进交底书：保留专利号、标题、**消化摘要后的**一两句方案概括（有 **`abstract`** 时概括须可追溯至该摘要）；**每条另起一行或表格列给出「来源 URL」**。避免大段抄袭权利要求或整段粘贴官方摘要。

## 输出契约（仅输出一个 JSON 围栏块）

```json
{
  "digests": [
    {
      "pub_no": "CN…A",
      "title": "照抄该条标题",
      "applicant": "申请方/来源机构（无则空）",
      "url": "照抄该条 link 字段",
      "scheme_summary": "消化改写后的技术方案概括（2-3 句，自己的话）",
      "application": "应用场景（1 句）",
      "limitation": "局限性（1-2 句，指向与本案相关的不足）",
      "abstract_used": true,
      "note": "无摘要时写「该条无摘要字段」等说明；可空"
    }
  ]
}
```

规则：逐条输出、顺序与注入一致；`scheme_summary` / `application` / `limitation` 为消化后的改写，**禁止**整段粘贴官方摘要；`abstract` 为空的条目 `abstract_used=false` 且 `note` 注明；与本案明显无关的条目也须输出，并在 `note` 写明「与本案主题相关度低」供用户勾选时参考。
