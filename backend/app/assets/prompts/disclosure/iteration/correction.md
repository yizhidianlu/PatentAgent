---
source_repo: handsomestWei/patent-disclosure-skill
source_path: prompts/disclosure/correction_handler.md
source_url: https://github.com/handsomestWei/patent-disclosure-skill
treatment: adapted
ported_version: 2026-08-25.1
notes: 纠正五分类（含附图与主题共六条分支）规则与「## 纠正摘要（留档）」输出格式逐字保留；执行门禁（Read/落盘/mermaid_render/matplotlib 反问）删除——由平台执行；文末新增纠正元信息 JSON 契约。
---

# 迭代 · 对话纠正（GEN 调用）

## 前提（平台已代办）

平台已注入：当前定稿全文（或受影响章节）、用户的纠正说明、术语表与 figure_plan（如有）。纠正结果由平台写入**新时间戳文件**（禁止覆盖旧稿）并维护修订对话记录；你只负责按下列步骤改稿与输出。

## 何时启用

由意图路由判定为 `correct` 时执行本流程：用户针对**已有交底书**指出**错误、与事实或参数不符、表述问题、保护点调整**等（例如「这里不对」「和 3.5 不一致」「保护点应强调 XXX」「某词太抽象」「标题有××但正文很少」）时，**应**执行本流程。**不要求**用户说出「迭代」等固定词；也**不必**先询问是否进入迭代模式。

## 步骤

1. **提取纠正点**：具体章节、原问题、用户期望，可摘要用户原话。
2. **分类**：
   - **事实与技术**：流程、参数、模块关系 → 改第三章及相关实施例、3.5。
   - **术语与可读性**：某词太抽象、与标题/话题背景不匹配 → 按**术语族替换**换成领域表述（1.1 至第六章及 mermaid）；3.1 定义领域词及如何判定。禁止只改一处、禁止做成全局禁词表、禁止靠定义保留错位词、禁止写入材料没有的配置模块。
   - **符号与公式体例**：上标维度（如 `^{cpu}`）、装饰音（`\tilde` 等）、符号多义、LaTeX 分隔符混用、3.5 与 3.4.1 不同形、未更新 `formula_plan` → 改 **formula_plan**、**3.4.1 符号表**、相关公式、**3.5 符号列**及第六章实施例；遵循公式体例规则（invention/formula_style.md §7.7）、范式库 references/formulas/ 与 template_reference.md §3.4.1 正/反例。
   - **查新与区别**：现有技术或区别论述不准 → 改第一章，必要时再检索。
   - **保护点与表述**：第四章、第五章论点 → 与第三章对齐，避免矛盾；保护点可跟场景词但须仍为可实施机制；实用/外观若**侧重点/主题转向**，按附图类同样 **先重评或新建 `figure_plan.yaml`**。
   - **附图与主题（实用/外观）**：换图、改件号/视图、主题转向 → **先更新或新建 `figure_plan.yaml`**（重排入文图、`covers`、`relates_to`），再改正文「如图/见图 N」与插图路径。
3. **落地修改**：形成纠正后全文，交平台写入新时间戳文件；**禁止**无必要大段重写无关章节；**勿默认覆盖**用户上一版文件名。
4. **自检**：平台将在纠正后执行对应类型的自检（self_check_*）。术语族替换后 §8.1 相关项必核。
5. **对话记录**：修订对话记录由平台自动追加，无须你输出。

## 输出（强制，不可省略）

在交付修改后的正文（或说明已写入路径）之后，**必须在同一条回复中**追加独立小节，**标题固定为**：

## 纠正摘要（留档）

其下用 **2–5 句完整中文**，说明：**修改位置**、**依据**、**是否影响保护点或检索**；若动过附图/主题，点明 **`figure_plan` 已同步**。若做了术语族替换，点明新旧叫法及是否整篇对齐。  
若未输出本节，视为未完成本 prompt。

**定稿延续**：本轮纠正结果作为交付定稿时，「权利要求偏向点」建议由平台在交付阶段以独立调用（invention/claim_bias.md）产出，本调用**不**输出该交互，也**不得**写入正文。

## 输出顺序与元信息契约

1. 纠正后的受影响章节全文（markdown；**禁止**无必要大段重写无关章节）。
2. 「## 纠正摘要（留档）」小节（格式与句数见上文，平台存档并展示）。
3. 文末**先**输出一个纠正元信息 ```json 围栏块（服务端剥离，不入正文）：

```json
{
  "corrections": [
    {"location": "3.5", "category": "符号与公式体例", "basis": "依据（用户原话或材料）"}
  ],
  "needs_search": false,
  "figure_plan_updated": false,
  "term_replacements": [{"old": "旧叫法", "new": "领域表述"}]
}
```

`category` 取步骤 2 的分类名（事实与技术 / 术语与可读性 / 符号与公式体例 / 查新与区别 / 保护点与表述 / 附图与主题）。公式类纠正会触发平台公式门禁复跑；术语类会按 `term_replacements` 做全文替换核查。随后按术语规则再输出 terms_delta 块。
